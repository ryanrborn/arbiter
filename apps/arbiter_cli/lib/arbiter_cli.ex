defmodule ArbiterCli do
  @moduledoc """
  `arb` — Elixir port of the Go `bd` CLI. Talks to the arbiter_web Phoenix
  REST API over local HTTP. See `ArbiterCli.Main` for the entry point and
  `ArbiterCli.Cmd.*` for individual subcommand modules.
  """
end
