[2026-06-25T18:22:59.112729Z]
VERDICT: REQUEST_CHANGES
Findings (enumerated):
1. **[HIGH] Runtime caller not updated — MCP `coordinator_inbox` returns a tuple instead of a count.**
   `apps/arbiter/lib/arbiter/mcp/tools.ex:125`
   ```elixir
   cleared = if clear, do: Message.clear_read("admiral", workspace_id: ws_id), else: 0
   ```
   `clear_read/2` now returns `{:ok, n, 0, unread}` instead of the integer `n`. So when `clear: true`, the `cleared` field in the tool response becomes `{:ok, 1, 0, 0}` (a tuple) while `clear: false` yields integer `0` — a type-inconsistent, wrong value exposed to the MCP coordinator tool. This is not caught by a test but is a live behavior regression.
   *Fix:* destructure the result, e.g. `cleared = if clear, do: (Message.clear_read("admiral", workspace_id: ws_id) |> then(fn {:ok, r, _, _} -> r end)), else: 0` (or return `deleted_read`/`deleted_unread`/`remaining_unread` in the tool payload to match the new contract).
2. **[HIGH] Existing unit tests for `clear_read/2` now fail (return value changed from int to tuple).**
   `apps/arbiter/test/arbiter/messages/message_test.exs:162` and `:178`
   ```
   left:  {:ok, 1, 0, 1}    right: 1
   ```
   Both `assert Message.clear_read("admiral") == 1` assertions fail.
   *Fix:* update assertions to `assert Message.clear_read("admiral") == {:ok, 1, 0, ...}` (matching the new contract), and add coverage for `clear_all/2`.
3. **[HIGH] Existing controller test fails — response key renamed `deleted` → `deleted_read`.**
   `apps/arbiter_web/test/arbiter_web/controllers/api/message_controller_test.exs:127`
   ```
   assert %{"data" => %{"deleted" => 1}} = json_response(conn, 200)
   ```
   The controller now returns `%{"deleted_read" => 1, "deleted_unread" => 0, "remaining_unread" => 1}`.
   *Fix:* update the assertion to the new shape and add a test for the `all=true` branch hitting `clear_all`.
4. **[LOW] Inconsistent `kind` filtering between `clear_read` and `clear_all`.**
   `apps/arbiter/lib/arbiter/messages/message.ex:277` vs `:312`
   `clear_read`'s read-deletion query filters only on `to_ref and not is_nil(read_at)` (no `kind in @mailbox_kinds`), but its unread *count* query and all of `clear_all` filter on `kind in @mailbox_kinds`. So a read message with a non-mailbox `kind` is destroyed by `clear_read` but its read-count peers are scoped differently, and `clear_all` would *not* delete it. Behavior is defensible (preserves the original `clear_read` scope) but the asymmetry is a latent surprise.
   *Fix:* apply the same `kind in @mailbox_kinds` scoping to both read-deletion queries (or document why `clear_read` is intentionally broader).
Findings 1–3 must be addressed (the branch does not currently pass its own test suite — `mix test` shows 3 failures). Finding 4 is optional but recommended.
arb done
⚙ claude session success · 64.1s · $0.4395
---
